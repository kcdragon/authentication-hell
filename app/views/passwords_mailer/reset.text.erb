************
Hi <%= @user.username %>,
************

You recently requested to reset the password for your Authentication Hell account. Use the link below to reset it. This link is only valid for the next <%= distance_of_time_in_words(0, @user.password_reset_token_expires_in) %>.

Reset your password ( <%= edit_password_url(@user.password_reset_token) %> )

If you did not request a password reset, you can safely ignore this email — your password will stay the same.

If you're having trouble with the link above, copy and paste the URL below into your web browser.

<%= edit_password_url(@user.password_reset_token) %>
