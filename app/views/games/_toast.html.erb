<%# locals: (toast:) %>

<div id="toast_<%= toast.fetch(:id) %>"
     class="game-toast w-[346px] max-w-[90vw] border-[3px] border-ink rounded-[3px] bg-totp text-white px-3.5 py-3 font-bold shadow-brut pointer-events-none">
  ↯ <%= toast.fetch(:message) %>
</div>
