******************
Welcome, <%= @user.username %>!
******************

Thanks for signing up for Authentication Hell. Confirm your email address with the link below to activate your account and start playing.

Confirm your email ( <%= email_confirmation_url(token: @token) %> )

This link will expire in 1 day.

If you're having trouble with the link above, copy and paste the URL below into your web browser.

<%= email_confirmation_url(token: @token) %>
